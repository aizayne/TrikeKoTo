name: Build Debug APK

on:
  push:
    branches: [main, master]
  workflow_dispatch: # lets you trigger manually from the Actions tab

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.7"
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build debug APK
        run: flutter build apk --debug

      # Rename so the downloaded file is "TrikeKoTo.apk" instead of the
      # default Flutter "app-debug.apk". Runs on Linux so we use mv.
      - name: Rename APK
        run: mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/TrikeKoTo.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TrikeKoTo
          path: build/app/outputs/flutter-apk/TrikeKoTo.apk
          if-no-files-found: error
          retention-days: 30
